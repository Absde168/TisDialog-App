package ui.theme

import androidx.compose.ui.graphics.Color

// ─── Brand palette (Figma exact) ──────────────────────────────────────────────
val Primary        = Color(0xFF214280)   // кнопки, заголовки, иконки
val PrimaryDark    = Color(0xFF172E5C)   // статус-бар, hover
val Secondary      = Color(0xFF3A6BC4)   // второстепенные акценты

// ─── Backgrounds ──────────────────────────────────────────────────────────────
val BackgroundLight = Color(0xFFFAFAFA)  // фон всех экранов (светлая тема)
val SurfaceWhite    = Color(0xFFFFFFFF)  // карточки (светлая тема)
val DarkBg          = Color(0xFF121212)  // фон (тёмная тема)
val DarkSurface     = Color(0xFF1E1E1E)  // карточки (тёмная тема)
val DarkSurfaceVariant = Color(0xFF2A2A2A) // альтернативные поверхности

// ─── Text ─────────────────────────────────────────────────────────────────────
val TextPrimary    = Color(0xFF000000)   // основной текст (светлая тема)
val TextSecondary  = Color(0xFF7E7E7E)   // вторичный текст (светлая тема)
val TextHint       = Color(0xFFA8A8A8)   // placeholder
val TextOnDark     = Color(0xFFE8E8E8)   // основной текст (тёмная тема)
val TextSecondaryDark = Color(0xFFAAAAAA) // вторичный текст (тёмная тема)

// ─── Semantic ─────────────────────────────────────────────────────────────────
val ErrorRed       = Color(0xFFFF3D3D)   // ошибки
val SuccessGreen   = Color(0xFF2E7D32)   // успех
val WhiteText      = Color(0xFFFFFFFF)   // текст на тёмном фоне
