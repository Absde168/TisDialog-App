package ui.theme

import androidx.compose.ui.graphics.Color

// ─── Brand palette (Figma exact) ──────────────────────────────────────────────
val Primary        = Color(0xFF214280)   // кнопки, заголовки, иконки
val PrimaryDark    = Color(0xFF172E5C)   // статус-бар, hover
val Secondary      = Color(0xFF3A6BC4)   // второстепенные акценты

// ─── Backgrounds ──────────────────────────────────────────────────────────────
val BackgroundLight = Color(0xFFFAFAFA)  // фон всех экранов
val SurfaceWhite    = Color(0xFFFFFFFF)  // карточки
val DarkBg          = Color(0xFF060606)  // тёмные элементы

// ─── Text ─────────────────────────────────────────────────────────────────────
val TextPrimary    = Color(0xFF000000)   // основной текст
val TextSecondary  = Color(0xFF7E7E7E)   // вторичный текст
val TextHint       = Color(0xFFA8A8A8)   // placeholder

// ─── Semantic ─────────────────────────────────────────────────────────────────
val ErrorRed       = Color(0xFFFF3D3D)   // ошибки
val SuccessGreen   = Color(0xFF2E7D32)   // успех
val WhiteText      = Color(0xFFFFFFFF)   // текст на тёмном фоне
